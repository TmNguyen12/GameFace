- args:
    sql: CREATE TABLE "games"("id" serial NOT NULL, "completed" boolean NOT NULL,
      "game_master" integer NOT NULL, "player2" integer NOT NULL, "player3" integer
      NOT NULL, "player4" integer NOT NULL, "player5" integer NOT NULL, PRIMARY KEY
      ("id") )
  type: run_sql
- args:
    name: games
  type: add_existing_table_or_view
